<% panel_id = 1 %>
<% presenter.grids.each_with_index do |grid, index| %>
    <% panel_id += index %>
    <div class="panel <%= 'active' if  panel_id == 1 %>" id="panel-<%= panel_id %>" role="tabpanel" aria-hidden="false">

    <% grid[:schedules_time].each do |schedules| %>

      <div class="mdc-layout-grid__inner">
        <div class="mdc-layout-grid__cell mdc-layout-grid__cell--span-1-desktop">
          <span class="schedule-time"><%= schedules.first %></span>
        </div>

        <% schedules.second.each do |schedule| %>
          <% if schedule.talk? %>
            <div class="mdc-layout-grid__cell mdc-layout-grid__cell--span-3-desktop">
              <%= render 'events/show/talk_card', talk: schedule.talk %>
            </div>
          <% elsif schedule.activity.description == 'A definir' %>
            <div class="mdc-layout-grid__cell mdc-layout-grid__cell--span-3-desktop">
              <%= render 'events/show/no_talk_card', activity: schedule.activity %>
            </div>
          <% else %>
            <div class="mdc-layout-grid__cell mdc-layout-grid__cell--span-11-desktop">
              <%= render 'events/show/interval_card', activity: schedule.activity %>
            </div>
          <% end %>
        <% end %>

      </div>

    <% end %>
    </div>

<% end %>
