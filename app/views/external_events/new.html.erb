<div class="row-fluid">
  <div class="span12">
    <div class="page-header">
      <h1><%= t("titles.external_event.new") %> <%= @talk.title %></h1>
    </div>
  </div>
</div>

<%= render 'form', :url => talks_external_events_path(@talk) %>
