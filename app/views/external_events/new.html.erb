<%= render "shared/header", index: :event %>
<%= render "shared/title", title: t("titles.external_event.title"), index: :event %>

<div class="container">
  <div class="card">
    <div class="card-body mx-4">
      <!--Header-->
      <div class="text-center">
          <h3 class="dark-grey-text mb-5"><strong><%= t("titles.external_event.new") %> <%= @talk.title %></strong></h3>
      </div>
      <%= render 'form', :url => talk_external_events_path(@talk) %>
    </div>
  </div>
</div>
