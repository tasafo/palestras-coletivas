<div class="container">
  <div class="card">
    <%= render "shared/title", title: t("titles.external_event.title") %>
    <div class="card-body">
      <h3 class="text-center"><%= t("titles.external_event.new") %> <%= @talk.title %></h3>
      <%= render 'form', url: talk_external_events_path(@talk) %>
    </div>
  </div>
</div>
