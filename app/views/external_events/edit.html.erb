<div class="row-fluid">
  <div class="span12">
    <div class="page-header">
      <h1><%= t("titles.external_event.edit") %> <%= @talk.title %></h1>
    </div>
  </div>
</div>

<%= render 'form', :url => talk_external_event_path(@talk, @external_event) %>
