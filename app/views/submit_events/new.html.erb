<div class="row-fluid">
  <div class="span12">
    <div class="page-header">
      <h1><%= t("titles.submit_event.new") %> "<%= @talk.title %>"</h1>
    </div>
  </div>
</div>

<%= render 'form', :url => talk_submit_events_path(@talk) %>
