<div class="container">
  <div class="card">
    <%= render "shared/title", title: t("titles.submit_event.title") %>
    <div class="card-body">
      <h2><%= t("titles.submit_event.new") %> <b><%= @talk.title %></b></h2>

      <%= render 'form', url: talk_submit_events_path(@talk) %>
    </div>
  </div>
</div>
