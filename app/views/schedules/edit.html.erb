<%= render "shared/title", title:  t("titles.schedules.index") %>

<div class="container">
  <div class="card">
    <div class="card-body mx-4">
      <h2><%= t("titles.schedules.edit") %> <b><%= @event.name_edition %></b></h2>

      <%= render 'form', url: event_schedule_path(@event, @schedule) %>
    </div>
  </div>
</div>
