<div class="container">
  <div class="card">
    <%= render "shared/title", title:  t("titles.schedules.index") %>
    <div class="card-body">
      <h2><%= t("titles.schedules.edit") %> <b><%= @event.name %></b></h2>

      <%= render 'form', url: event_schedule_path(@event, @schedule) %>
    </div>
  </div>
</div>
