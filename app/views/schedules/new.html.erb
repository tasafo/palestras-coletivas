<div class="row-fluid">
  <div class="span12">
    <div class="page-header">
      <h1><%= t("titles.schedules.new") %>: <%= "#{@event.name} - #{@event.edition}" %></h1>
    </div>
  </div>
</div>

<%= render 'form', :url => new_schedule_path %>
