<div class="row-fluid">
  <div class="span12">
    <div class="page-header">
      <h1><%= t("titles.enrollments.edit") %>: <%= "#{@event.name} - #{@event.edition}" %></h1>
    </div>
  </div>
</div>

<%= render 'form', :url => event_enrollment_path %>
