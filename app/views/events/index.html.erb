<% if logged_in? %>
  <%= link_to t("actions.new"), new_event_path, :class => "btn btn-primary" %>
  <br />
<% end %>

<% unless @events.blank? %>
  <h1><%= t("titles.events.index") %> (<%= @events.count %>)</h1>
  
  <%= render "shared/events" %>
<% else %>
  <h1><%= t("index.event.no_events") %></h1>
<% end %>