  <section id="about">
    <%= render "events/show/actions" %>

    <div class="container">
      <h2 class="text-primary"><%= t("labels.event.about") %></h2>

      <div class="text"><%= sanitize(@event.description) %></div>
    </div>
  </section>
