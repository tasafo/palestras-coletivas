    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li>
              <%= link_to t("app.name"), root_path, class: "scroll" %>
            </li>
            <li>
              <%= link_to t("labels.event.about"), "#about", class: "scroll" %>
            </li>
            <li>
              <%= link_to t("titles.schedules.index"), "#schedule", class: "scroll" %>
            </li>
            <li>
              <%= link_to t("show.event.place"), "#location", class: "scroll" %>
            </li>

            <% if @event && !@event.users.blank? %>
              <li>
                <%= link_to t("show.event.organizers"), "#organizers", class: "scroll" %>
              </li>
            <% end %>

            <li>
              <%= link_to t("show.event.enrolled"), "#presents", class: "scroll" %>
            </li>

            <% if @event && @event.issue_certificates %>
              <li>
                <%= link_to t("show.event.certificates"), "#certificates", class: "scroll" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <hr />
