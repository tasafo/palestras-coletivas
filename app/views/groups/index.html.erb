<% if logged_in? %>
  <%= link_to t("actions.new"), new_group_path, :class => "btn btn-primary" %>
  <br />
<% end %>

<% unless @groups.blank? %>
  <h1><%= t("titles.groups.index") %> (<%= @groups.count %>)</h1>
  
  <%= render "shared/groups" %>
<% else %>
  <h1><%= t("index.group.no_groups") %></h1>
<% end %>