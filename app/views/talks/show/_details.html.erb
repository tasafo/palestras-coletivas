<h2><%= @talk.title %></h2>

<% unless @presentation.frame %>
  <p class="text-warning"><%= t("show.talk.without_presentation") %></p>
<% end %>

<div class="trix-content"><%= sanitize(@talk.description) %></div>
