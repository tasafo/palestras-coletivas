<%= render "shared/header", index: :talk %>
<%= render "shared/header", title: t("titles.talks.edit"), index: :talk %>

<div class="container">
  <div class="card">
    <div class="card-body mx-4">
      <!--Header-->
      <div class="text-center">
          <h3 class="dark-grey-text mb-5"><strong>Editar Palestra</strong></h3>
      </div>
      <%= render 'form', :url => talk_path %>
    </div>
  </div>
</div>
