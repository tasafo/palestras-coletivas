<%= render "shared/header", index: :event %>

<%= render "shared/title", title: t("titles.export_subscribers.index"), index: :event %>

<div class="container">
  <div class="card">
    <div class="card-body mx-4">
      <h2><%= t("titles.export_subscribers.new") %> <b><%= @event.name_edition %></b></h2>

      <%= render 'form' %>
    </div>
  </div>
</div>
